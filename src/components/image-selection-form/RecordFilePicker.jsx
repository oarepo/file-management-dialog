// Accepts props for which mime types to accept
// accepts string (filter): pdf or images (or other image formats)
// accepts files object (files) from the AppContext
// accepts onChange callback when user chooses files (clicked on button)
// RecordFilePickerItem for each file